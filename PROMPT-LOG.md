# Prompt Log — Gradient Descent Playground

> เทมเพลตนี้ใช้สำหรับคัดลอกไปวางใน Google Doc แล้วส่งเป็นลิงก์ "Prompt Log" ตามที่อาจารย์กำหนด
> กรอกเฉพาะช่องที่มีเครื่องหมาย `_______` หรือ `⬜ รอเติม...` — **ห้ามลบหัวข้อใด ๆ ออก แม้ยังไม่มีเนื้อหา**

---

## ส่วน A — ข้อมูลโปรเจกต์

- **ชื่อ-นามสกุล:** ภาณุชัย พุ่มประเสริฐ
- **รหัสนักศึกษา:** 6704022617201
- **ชื่อแอป:** Gradient Descent Playground
- **แอปนี้ทำอะไร:**

  แอปนี้เป็น web app หน้าเดียวที่ทำให้เห็นภาพว่า Gradient Descent ทำงานอย่างไรบนพื้นผิว `f(x, y)` แบบ 3 มิติ ที่หมุนดูได้รอบด้าน ผู้ใช้เลือกพื้นผิวเป้าหมายได้ 3 แบบ — `x²+y²` (Bowl, convex มี global minimum จุดเดียว), `x²−y²` (Saddle, ไม่มี minimum เลย จุด (0,0) เป็น saddle point) และ Himmelblau `(x²+y−11)²+(x+y²−7)²` (มี global minima 4 จุดที่ค่า f = 0 เท่ากันหมด) แล้วปรับ learning rate ด้วย slider แบบ log scale หรือกดปุ่ม preset เพื่อดูว่าค่ามากไป น้อยไป หรือพอดี ส่งผลต่อการลู่เข้าอย่างไร

  เพราะเป็น 2 ตัวแปร อนุพันธ์จึงกลายเป็น gradient vector `∇f = (∂f/∂x, ∂f/∂y)` ซึ่งใกล้กับ gradient descent ที่ใช้จริงใน machine learning มากกว่ากรณีตัวแปรเดียว บนผิวมีเส้น contour (level set) วาดทับ ทำให้เห็นว่า **gradient ตั้งฉากกับ contour เสมอ** และมีกราฟ f ต่อ step อยู่ในแถบตัวเลข ซึ่งก็คือ convergence curve ที่ใช้ดูกันจริงเวลาเทรนโมเดล

  **หมายเหตุเรื่องลำดับการพัฒนา:** โปรเจกต์นี้เริ่มจากเวอร์ชัน 1 มิติก่อน (ยังเก็บไว้ในโปรเจกต์ที่ไฟล์ `index-1d.html`) ซึ่งเป็นตัวที่ prompt แรกในส่วน B และการแก้บั๊กรอบที่ 1–7 ในส่วน C บันทึกไว้ แล้วจึงต่อยอดเป็นเวอร์ชัน 3 มิติในรอบถัด ๆ ไป บทเรียนจากทั้ง 7 รอบแรกถูกยกมาเป็นข้อกำหนดตั้งแต่ต้นของเวอร์ชัน 3 มิติ

- **เครื่องมือ AI ที่ใช้:** Claude

---

## ส่วน B — Prompt แรก

```
บริบท:
ฉันเป็นนักศึกษาปี 3 กำลังทำโปรเจกต์สำหรับวิชา Prompt Engineering for Coding
ต้องการ web app หน้าเดียวชื่อ "Gradient Descent Playground" ที่ทำให้ผู้ใช้
เห็นภาพและเข้าใจได้ด้วยสัญชาตญาณว่า Gradient Descent (แบบ 1 มิติ) ทำงานอย่างไร
และทำไม learning rate ถึงสำคัญมากต่อการลู่เข้า (convergence)

กติกา / พฤติกรรมของแอป:
1. มีฟังก์ชันเป้าหมายให้เลือก 3 แบบ พร้อมอนุพันธ์ (derivative) ที่ถูกต้องของแต่ละฟังก์ชัน:
   - f(x) = x²          →  f'(x) = 2x
   - f(x) = x⁴ − 3x² + 1 →  f'(x) = 4x³ − 6x
   - f(x) = x² + 3·sin(2x) → f'(x) = 2x + 6·cos(2x)
2. มี slider ปรับค่า learning rate โดยใช้ log scale (เพราะ learning rate ที่มีความหมาย
   มักอยู่ในช่วงหลายเท่าตัว เช่น 0.001 ถึง 2 การใช้ scale เชิงเส้นธรรมดาจะทำให้ปรับค่าน้อย ๆ ได้ยาก)
3. มีปุ่ม preset 3 ปุ่ม ตั้งค่า learning rate ทันทีเมื่อกด:
   - "0.01 เล็กไป" → ตั้ง learning rate = 0.01
   - "0.1 กำลังดี" → ตั้ง learning rate = 0.1
   - "1.05 ใหญ่ไป" → ตั้ง learning rate = 1.05
4. มีปุ่มควบคุมการรัน: "Step" (เดินหน้าอัปเดตค่า x ทีละ 1 สเต็ปด้วยสูตร
   x_new = x_old − learning_rate × f'(x_old)), "Run" (เดินสเต็ปต่อเนื่องอัตโนมัติจนกว่าจะกดหยุดหรือค่าลู่เข้า/ลู่ออกชัดเจน),
   และ "Reset" (กลับไปที่จุดเริ่มต้น ล้างเส้นทางที่ผ่านมา)
5. ผู้ใช้คลิกลาก (click-and-drag) บน canvas เพื่อย้ายตำแหน่งจุดเริ่มต้น (x0) ได้โดยตรง
6. รองรับการควบคุมด้วยคีย์บอร์ด (เช่น ลูกศรซ้าย/ขวาขยับจุดเริ่มต้น, spacebar หรือปุ่มคล้ายกันสำหรับ step/run)
7. วาดกราฟของฟังก์ชัน, จุดปัจจุบัน, เส้นทาง (trajectory) ที่ลูกบอลเคยเดินผ่านมาทั้งหมด,
   และเส้นสัมผัส (tangent line) ที่จุดปัจจุบันเพื่อให้เห็นทิศทางและขนาดของ gradient
8. ต้องมีคำอธิบายวิธีใช้งานแสดงอยู่ในหน้าเว็บเลย (ไม่ใช่แค่ในโค้ดหรือ README) สั้น กระชับ
   เข้าใจได้แม้ไม่เคยเรียน Gradient Descent มาก่อน

หน้าตา:
- ธีมมืด (dark theme) ทั้งหน้า พื้นหลังโทนเข้ม ตัวกราฟและตัวหนังสือมีคอนทราสต์ชัดเจนอ่านง่าย
- เลย์เอาต์ต้องดูเป็นมืออาชีพ ไม่รก มีลำดับความสำคัญของสายตาที่ชัดเจน: กราฟเป็นจุดเด่นหลัก
  ส่วนควบคุม (เลือกฟังก์ชัน, slider, ปุ่ม preset, ปุ่ม step/run/reset) อยู่รวมกันเป็นระเบียบ
- แสดงค่าปัจจุบันแบบตัวเลขด้วย เช่น x ปัจจุบัน, f(x) ปัจจุบัน, learning rate ปัจจุบัน, จำนวนสเต็ปที่ผ่านมา

ข้อจำกัดทางเทคนิค:
- เขียนเป็นไฟล์เดียว (single file) คือ index.html เท่านั้น รวม HTML + CSS + JavaScript ไว้ในไฟล์เดียวกัน
- ห้ามใช้ library หรือ framework ภายนอกใด ๆ ทั้งสิ้น (ห้ามใช้ React, Vue, jQuery, Chart.js, ฯลฯ)
  ใช้ vanilla JavaScript และ HTML5 Canvas (หรือ SVG) ล้วน ๆ เท่านั้น
- ต้องรองรับหน้าจอมือถือ (responsive): canvas ต้องย่อขนาดตามความกว้างหน้าจอ,
  การลากบน canvas บนมือถือต้องไม่ทำให้หน้าเว็บเลื่อน (ต้องป้องกัน touch scroll ระหว่างลาก)
- ต้องรองรับจอความละเอียดสูง (คูณด้วย devicePixelRatio ตอนวาด canvas เพื่อไม่ให้ภาพเบลอ)
- ต้องระวังกรณี learning rate มากเกินไปจนค่า x หรือ f(x) กลายเป็น Infinity หรือ NaN
  แล้วทำให้กราฟหรือลูกบอลหลุดออกนอกจอ ให้ออกแบบการจัดการเคสนี้ไว้ด้วย (เช่น จำกัดขอบเขตการวาด
  หรือแสดงข้อความเตือนเมื่อค่าลู่ออก)
- ไม่ต้องมี backend, ไม่ต้องมี build step, เปิดไฟล์ index.html ในเบราว์เซอร์แล้วใช้งานได้ทันที

สไตล์โค้ด:
- เขียนโค้ด JavaScript ให้อ่านง่าย มีคอมเมนต์อธิบายส่วนสำคัญ (โดยเฉพาะสูตรคณิตศาสตร์และ
  ส่วนที่แปลงพิกัดคณิตศาสตร์เป็นพิกัด canvas)
- ตั้งชื่อตัวแปรและฟังก์ชันให้สื่อความหมาย ไม่ใช้ชื่อย่อที่เดายาก
- แยกส่วนคำนวณ (math), การวาด (render), และการจัดการ event ให้เป็นสัดส่วนชัดเจนในโค้ด
```

### ทำไมถึงเขียน prompt แบบนี้

- **บริบท** — บอก AI ว่าเป็นงานของวิชาอะไรและเป้าหมายการเรียนรู้คืออะไร (ไม่ใช่แค่ "ทำเว็บ gradient descent")
  เพื่อให้ AI เลือกความซับซ้อนและคำอธิบายให้เหมาะกับคนที่ยังไม่เก่ง ไม่ใช่เขียนแบบงานวิจัย
- **กติกา/พฤติกรรม** — ระบุสูตรอนุพันธ์ตรง ๆ ในตัว prompt เอง เพราะไม่อยากเสี่ยงให้ AI คำนวณอนุพันธ์ผิด
  (ยิ่งฟังก์ชันมี sin/cos ยิ่งเสี่ยงพลาดเครื่องหมายหรือค่าคงที่) และระบุสูตร update rule ตรง ๆ
  เพื่อไม่ให้ตีความสูตร gradient descent ผิด
- **หน้าตา** — บอกธีมมืดและลำดับความสำคัญของสายตาให้ชัด เพราะถ้าไม่บอก AI มักจะยัดทุกอย่างเรียงกันแบบฟอร์ม
  ไม่ได้ทำให้กราฟเป็นจุดเด่น
- **ข้อจำกัดทางเทคนิค** — ใส่เคส edge case ที่คาดว่าจะเจอไว้ล่วงหน้าเลย (learning rate ใหญ่จนค่าเป็น Infinity,
  devicePixelRatio, touch scroll บนมือถือ) เพราะรู้จากประสบการณ์ว่าเป็นจุดที่ AI มักลืมถ้าไม่เตือนไว้ก่อน
  การเจอปัญหาพวกนี้ตอน debug ทีหลังเสียเวลากว่าการป้องกันไว้ในเชิง prompt
- **สไตล์** — ขอคอมเมนต์และการแยกส่วนโค้ดให้ชัด เพราะตัวเองยังไม่แข็งเรื่อง OOP/coding fundamentals
  การมีโค้ดที่อ่านง่ายและแยกส่วนจะช่วยให้ตามแก้บั๊กเองได้ง่ายขึ้นในรอบถัด ๆ ไป

### AI ให้ผลลัพธ์ออกมาอย่างไร

รัน prompt นี้แบบคำต่อคำในเซสชัน Claude Code (1 ก.ย. 2569 21:53) ได้ `index.html` ไฟล์เดียว ~370 บรรทัด
ไฟล์ดิบเก็บไว้ที่ `rounds/v1-raw.html` (ยังไม่แก้อะไรเลย) ผ่าน `node --check` ไม่มี syntax error

**ตรงตาม prompt:** ฟังก์ชันครบ 3 แบบและอนุพันธ์ถูกทุกตัว · slider log scale (slider 0→1 ให้ lr 0.001→2 จริง) ·
ปุ่ม preset 3 ปุ่ม · Step/Run/Reset · ลากบน canvas ย้ายจุดเริ่มต้น · คีย์บอร์ด (ลูกศร/space/R) ·
วาดเส้นทางและเส้นสัมผัส · คำอธิบายวิธีใช้อยู่ในหน้าเว็บจริง · ธีมมืด · ไม่ใช้ library ภายนอกเลย ·
คูณ devicePixelRatio และใส่ `touch-action: none` ให้ตามที่ขอ · แยกส่วน math / render / event ชัดเจนตามที่ขอ

**ไม่ตรง / ยังพลาด:** ข้อจำกัดเรื่อง "ระวังค่าลู่ออกจนหลุดจอ" AI ทำมาแค่ครึ่งเดียว —
ดักค่า Infinity ไว้จริง แต่ตั้งเกณฑ์ที่ `\|x\| > 1e6` ซึ่งหลวมเกินกว่าที่ตาจะทัน (ดูรอบ 2)
และไม่ได้เช็คเลยว่าจุดที่ความชันเป็นศูนย์นั้นเป็นจุดต่ำสุดหรือจุดสูงสุด (ดูรอบ 1)
สรุปคือ prompt ที่ระบุ edge case ไว้ล่วงหน้า **ช่วยได้แต่ไม่พอ** ถ้าไม่ได้บอกเกณฑ์ที่วัดได้จริง

---

## ส่วน C — บันทึกการปรับแก้

> ⚠️ **ต้องคัดลอก prompt จริงทุกรอบทันทีที่พิมพ์ อย่ารอไปเขียนย้อนหลัง** —
> ถ้าย้อนเขียนทีหลังมักจำคำผิด หรือแต่งให้ดูเป็นเหตุเป็นผลเกินจริง ซึ่งไม่ตรงกับสิ่งที่เกิดขึ้นจริง
> และผู้ตรวจจะสังเกตได้ว่า prompt log ไม่สมจริง

> 📌 **ปัญหาในตารางข้างล่างเป็นแค่ "สิ่งที่คาดว่าจะเจอ" ไม่ใช่บทที่ต้องเดินตาม**
> prompt แรกในส่วน B ระบุเรื่อง Infinity/NaN, devicePixelRatio และ touch scroll ไว้ครบแล้ว
> ถ้า AI ทำถูกตั้งแต่รอบแรก **ถือเป็นเรื่องดี** เพราะแปลว่า prompt แรกมีคุณภาพ (= คะแนนส่วน B ซึ่งหนัก 35%)
> ให้ลบแถวนั้นทิ้งแล้วใส่ปัญหาที่เจอจริงแทน — **ห้ามเขียนว่าเจอปัญหาที่ไม่ได้เจอ**
> ยังไงก็ตามงาน canvas มีจุดพังเสมอ (สเกลแกน, เส้นสัมผัสยาวเกิน, ปุ่มล้นที่จอ 360px, การ map ค่า slider แบบ log)
> เดี๋ยวก็มีของจริงให้เขียนครบ 5 รอบเอง

| รอบ | ปัญหาที่เจอ | Prompt ที่ใช้แก้ | ผลลัพธ์ | บทเรียน |
|---|---|---|---|---|
| 1 ★ (รอบ debug) | เลือก `x⁴−3x²+1` กด preset `0.1 กำลังดี` (เริ่มที่ x = −2.0) แอปเดิน **2 สเต็ปแล้วหยุด** ที่ x = 0 ถือว่าลู่เข้าแล้ว — แต่ x = 0 เป็น**จุดสูงสุดเฉพาะที่** (f″(0) = −6) จุดต่ำสุดจริงอยู่ที่ x = ±1.2247, f = −1.25 คือหยุดที่จุดที่สูงกว่าคำตอบจริง 2.25 | "แก้ให้เช็คด้วยว่าจุดที่ความชันเป็น 0 เป็นจุดต่ำสุดจริงไหม" | AI เพิ่มฟังก์ชัน `classifyStationary()` ที่ชิมค่า f ที่ x−0.001 และ x+0.001 แล้วเทียบกับ f(x): ต่ำกว่าทั้งสองข้าง = จุดต่ำสุด, สูงกว่าทั้งสองข้าง = จุดสูงสุด, อย่างละข้าง = จุดอานม้า ถ้าไม่ใช่จุดต่ำสุดจะสะกิด x ไปทางที่ f ต่ำกว่าแล้วเดินต่อ พร้อมขึ้นข้อความบอกผู้ใช้ **ผลทดสอบ:** เคสเดิม (x⁴−3x²+1, lr = 0.1, เริ่มที่ −2.0) เดิมหยุดที่ x = 0, f = 1 ใน 2 สเต็ป ตอนนี้เดินต่อไปจบที่ x = 1.2247, f = −1.25 ใน 27 สเต็ป ซึ่งเป็นจุดต่ำสุดจริง · ตรวจซ้ำอีก 2 ฟังก์ชันไม่มีอะไรพัง (x² 80→79 สเต็ป, x²+3sin(2x) 20→19 สเต็ป ได้คำตอบเดิม จำนวนสเต็ปต่างไป 1 เพราะเงื่อนไขหยุดเปลี่ยนจากเช็คความชันที่จุดเก่าเป็นจุดใหม่) | สั่งให้ "เช็คเงื่อนไข" อย่างเดียวได้ผลเลย เพราะบอกไปตรง ๆ ว่าจะให้เช็คอะไร ไม่ต้องบอกวิธีทำ · บทเรียนคณิตศาสตร์คือ f′(x) = 0 ไม่ได้แปลว่าเจอคำตอบ มันแค่บอกว่าถึงจุดวิกฤต ต้องดู f″ หรือชิมค่าสองข้างถึงจะรู้ว่าต่ำสุดหรือสูงสุด — gradient descent ไม่รู้เรื่องนี้เองโดยธรรมชาติ · และ prompt แรกไม่ได้พูดถึงเรื่องนี้เลย AI จึงเขียนเงื่อนไข `\|f′\| < 1e-7 แล้วหยุด` ซึ่ง "ดูสมเหตุสมผล" แต่ผิดในเชิงคณิตศาสตร์ |
| 2 | เลือก `x²` กด preset `1.05 ใหญ่ไป` ลูกบอลหลุดออกนอกกรอบกราฟตั้งแต่สเต็ปต้น ๆ แต่ข้อความเตือน "ลู่ออก" ขึ้นตอน**สเต็ปที่ 133** — ระหว่างนั้นจอว่างเปล่าโดยไม่บอกอะไรผู้ใช้เลย | ไม่ได้พิมพ์บรรยาย แต่**แปะค่าที่อ่านได้จากหน้าจอจริงไปให้ดูตรง ๆ**: `ค่าลู่ออก (diverged) — learning rate ใหญ่เกินไป กด Reset แล้วลองค่าน้อยลง / x 960170.549 / f(x) 921927482742.489 / learning rate 1.050 / steps 133` | AI อ่านตัวเลขออกเองว่าปัญหาคือ**เกณฑ์ตรวจจับช้าไป** ไม่ใช่ตรวจไม่เจอ แล้วเปลี่ยนจากรอให้ `\|x\| > 1e6` เป็นตรวจว่า "x ออกนอกกรอบกราฟแล้ว **และ** ก้าวถัดไปยิ่งไกลกว่าเดิม" พร้อมเขียนเหตุผลลงในข้อความเตือนด้วย **ผลทดสอบ:** `x²` + 1.05 เตือนที่**สเต็ปที่ 5** (x = 3.22) แทนสเต็ปที่ 133 · `x⁴−3x²+1` และ `x²+3sin(2x)` เตือนตั้งแต่**สเต็ปที่ 1** · ตรวจซ้ำกับ preset 0.1 ทั้ง 3 ฟังก์ชัน ไม่มีอันไหนถูกตัดสินว่าลู่ออกผิด ๆ | **แปะผลลัพธ์จริงให้ AI ดู ได้ผลดีกว่าบรรยายเป็นคำ** — ไม่ต้องอธิบายเลยว่าอยากได้อะไร แค่ให้ตัวเลขไป AI วินิจฉัยเองได้ว่าเงื่อนไขเดิมหลวมตรงไหน · และเงื่อนไขที่ "ถูกต้องทางคณิตศาสตร์" (1e6 ก็คือ Infinity จริง ๆ นั่นแหละ) ไม่ได้แปลว่า "ใช้ได้จริงกับคน" เพราะคนดูจอ ไม่ได้ดูตัวเลข |
| 3 | กด preset `0.01 เล็กไป` แล้วกด Run — ใช้ถึง **887 สเต็ป** กว่าจะลู่เข้า ระหว่างนั้นไม่มีอะไรบอกว่ายังเดินอยู่หรือค้างไปแล้ว และไม่มีเพดานจำนวนสเต็ป ถ้า lr เล็กกว่านี้ก็วิ่งไปเรื่อย ๆ ไม่มีวันจบ | แปะค่าจากหน้าจออีกสถานะหนึ่งไปในข้อความเดียวกับรอบ 2: `พบจุดต่ำสุด: x = -0.0000, f(x) = 0.0000 / x -0.000 / f(x) 0.000 / learning rate 0.010 / steps 887` | AI เพิ่มเพดาน `MAX_STEPS = 1000` และเพิ่มแถบสถานะระหว่างเดิน (`กำลังเดิน... สเต็ปที่ N ความชัน = ...`) ถ้าเดินครบเพดานแล้วยังไม่ถึงจุดต่ำสุดจะหยุดแล้วขึ้นข้อความว่า learning rate เล็กเกินไป **ผลทดสอบ:** lr = 0.001 หยุดที่ 1000 สเต็ป (x = −0.270 ความชันยังเหลือ −5.4e−1) พร้อมข้อความอธิบาย · lr = 0.01 ยังลู่เข้าได้ตามปกติที่ 867 สเต็ป (ต่ำกว่าเพดาน) · ค่า 867 ในการจำลองต่างจาก 887 ที่เห็นบนจอเล็กน้อย เพราะการจำลองเริ่มที่ x = −2.0 พอดี ส่วนบนจอจุดเริ่มต้นอาจถูกลากไปแล้ว | บั๊กนี้ไม่ใช่โค้ดผิด — โค้ดทำถูกตามที่สั่งทุกอย่าง แต่**พฤติกรรมของอัลกอริทึมกับความอดทนของคนใช้ไม่ตรงกัน** · การกำหนดเพดานเป็นการตัดสินใจเชิงออกแบบ ไม่ใช่เชิงคณิตศาสตร์ AI ตัดสินใจแทนไม่ได้ถ้าไม่บอก · ตอนเขียน prompt แรกนึกถึงแต่กรณี "ใหญ่เกินไปจนพัง" ไม่ได้นึกถึง "เล็กเกินไปจนน่าเบื่อ" ทั้งที่มันคือครึ่งหนึ่งของสิ่งที่แอปนี้ตั้งใจจะสอน |
| 4 | หลังลู่เข้าแล้ว แถบข้อความขึ้นว่า `พบจุดต่ำสุด: x = -0.0000, f(x) = 0.0000` และช่องตัวเลขโชว์ `x = -0.000` — **"ลบศูนย์"** ซึ่งไม่มีความหมายทางคณิตศาสตร์ ทำให้ดูเหมือนแอปคำนวณพลาด ทั้งที่คำตอบถูกแล้ว | แปะข้อความที่เห็นบนจอมาให้ดู: `พบจุดต่ำสุด: x = -0.0000, f(x) = 0.0000` | สาเหตุคือค่าจริงเป็นลบจิ๋ว ๆ (x = −4.4171e−8) พอเรียก `.toFixed(4)` JavaScript ปัดเป็นศูนย์แต่**เก็บเครื่องหมายลบไว้** AI เพิ่มฟังก์ชัน `fmt(v, digits)` ที่ปัดก่อนแล้วเช็คว่าผลเป็นศูนย์หรือไม่ ถ้าใช่ให้ใช้ 0 บวก **ผลทดสอบ:** −4.4171e−8 → `0.0000` (เดิม `-0.0000`) · ค่าลบจริงไม่ถูกแตะ: −0.0004 → `-0.0004`, −1.25 → `-1.2500`, −0.6724 → `-0.6724` | บั๊กที่ **AI ไม่มีทางเจอเองเพราะมันไม่ได้มองจอ** ต้องมีคนเปิดดูถึงจะเห็น · เป็นบั๊กเชิงการนำเสนอ ไม่ใช่เชิงคำนวณ — ตัวเลขถูกมาตลอด ผิดแค่ตอนแสดงผล ซึ่งเป็นประเภทที่ test อัตโนมัติมักจับไม่ได้ · จุดที่พลาดง่ายคือถ้าแก้แบบมักง่าย (เช่น เจอ "-0" แล้วตัดเครื่องหมายลบทิ้ง) จะพังกับค่าลบจริง จึงต้องเทียบหลังปัดแทน |
| 5 | `keydown` ระดับ window แย่งปุ่มลูกศรไปจากตัวควบคุมที่โฟกัสอยู่ — Tab ไปที่ slider แล้วกดลูกศร learning rate **ไม่ขยับเลย** แต่จุดเริ่มต้นเลื่อนและกราฟ reset แทน · ที่กลุ่ม radio ก็เหมือนกัน ทำให้ **ฟังก์ชันที่ 2 และ 3 เข้าถึงด้วยคีย์บอร์ดล้วนไม่ได้เลย** ทั้งที่ prompt แรกสั่งไว้ว่า "ต้องรองรับการควบคุมด้วยคีย์บอร์ด" | `"กระจายงานให้ agent ทำให้ทำให้เร็วขึ้น"` — สั่งให้แตก agent ไปไล่ตรวจโค้ดขนานกัน ตัวหนึ่งดูอัลกอริทึม อีกตัวดู UI/event/responsive แล้วบังคับให้แยกให้ชัดว่าข้อไหน "ยืนยันแล้ว" ข้อไหน "ยังต้องเปิดเบราว์เซอร์ดู" | agent ฝั่ง UI ไล่อ่านครบ 425 บรรทัดแล้วรายงาน 15 ข้อ (สูง 3 กลาง 7 ต่ำ 5) และชี้ว่า 4 ข้อมาจาก**รากเดียวกัน** คือ handler เรียก `e.preventDefault()` โดยไม่เช็ค `e.target` เลย **การแก้:** เพิ่มการเช็คว่าถ้า target เป็น INPUT/BUTTON/SELECT/TEXTAREA ให้ `return` ทันที ปล่อยให้ตัวควบคุมจัดการคีย์ตัวเอง · ข้าม event ที่มี Ctrl/Meta/Alt เพื่อไม่ไปทับคีย์ลัดเบราว์เซอร์ · ใส่ `tabindex="0"` และ `aria-label` ให้ canvas เพราะเดิมคำสั่งลูกศรเป็น "คำสั่งลอย" ที่ไม่ผูกกับ element ไหน **ผลทดสอบ:** preset ทั้ง 5 ชุดยังได้ผลเท่าเดิมทุกตัว (79 / 27 / 19 / 5 / 867 สเต็ป) | บั๊กที่ **ไม่มีทางเจอด้วยการกดเล่นด้วยเมาส์** ต้องมีคนไล่อ่านโค้ดหรือใช้คีย์บอร์ดล้วนถึงจะเจอ · การกระจายงานให้ agent หลายตัวได้ผลเพราะ**แยกขอบเขตชัด** ตัวหนึ่งห้ามแตะตัวเลข อีกตัวห้ามแตะ UI ไม่งั้นจะได้รายงานซ้ำกัน · ที่สำคัญคือสั่งให้แยก "ยืนยันแล้ว" กับ "ยังเดา" ออกจากกัน เพราะ agent เปิดเบราว์เซอร์ไม่ได้เหมือนกัน ถ้าไม่บังคับมันจะเขียนรวมกันจนแยกไม่ออกว่าอะไรจริง |
| 6 | จากรายงาน agent ตัวเดียวกัน อีก 2 ข้อ: (ก) จุดเริ่มต้นหลุดออกนอกกรอบกราฟได้ไม่จำกัด ลูกบอลหายจากจอ **แล้วกด Reset ก็ไม่กลับมา** เพราะ `reset()` ดึงค่ากลับไปที่ `x0` ตัวที่หลุดขอบอยู่ดี (ข) ตอนลู่ออก ข้อความเขียวอันเก่าค้างโชว์คู่กับแถบแดง กลายเป็นสองข้อความขัดแย้งกันเอง | ต่อจาก prompt เดียวกับรอบ 5 — เลือกหยิบเฉพาะข้อที่รุนแรงสูงกับข้อที่ทำให้ผู้ใช้สับสนมาแก้ ไม่ได้แก้ทั้ง 15 ข้อ | เพิ่มฟังก์ชัน `clampStart()` แล้วเรียกที่ต้นทาง `reset()` ทำให้ทุกเส้นทางที่แก้ `x0` ถูกจำกัดเหมือนกันหมด (เดิม clamp แค่ตอนลากบน canvas ที่เดียว ส่วนลูกศรกับการสลับฟังก์ชันไม่ clamp) · และเซ็ต `state.status = ""` ก่อน `return` ในทั้งสองเส้นทางที่ตรวจพบการลู่ออก **ผลทดสอบ:** ลากไปซ้ายสุดที่ `x²+3sin(2x)` (x₀ = −4) แล้วสลับไป `x²` ได้ x₀ = −3 พอดีขอบ (เดิมค้างที่ −4 นอกกรอบ) · กดลูกศรซ้าย 300 ครั้งได้ −3.000 (เดิมได้ −18.0) · ลู่ออกแล้วเหลือแถบแดงอันเดียว | บทเรียนเรื่องโครงสร้าง: การ clamp ไว้ที่ "ทางเข้าเดียว" แล้วนึกว่าครอบคลุมแล้ว เป็นกับดักที่เจอบ่อย — ต้องวางไว้ที่จุดที่**ทุกเส้นทางต้องผ่าน** (ในที่นี้คือ `reset()`) ไม่ใช่ที่ handler ใดhandlerหนึ่ง · และ agent รายงานมา 15 ข้อ แต่เลือกแก้แค่ที่รุนแรงจริง เพราะเวลาจำกัด — การตัดสินใจว่า**อะไรไม่ต้องแก้**ก็เป็นส่วนหนึ่งของงาน |
| 7 | agent ฝั่งอัลกอริทึมรายงาน 8 ข้อ ตรวจกับเวอร์ชันล่าสุดแล้วเหลือของจริง 3 ข้อ: (ก) **ตัวนับ `steps` เดินต่อหลังประกาศว่าเจอจุดต่ำสุดแล้ว** เคาะ Spacebar อีก 100 ครั้งได้ 79 → 179 ทั้งที่ x ขยับแค่ 9e−18 (ข) เส้นทางมีก้าวปลอม `path.length = steps + 2` เพราะการสะกิดหนีจุดสูงสุดถูกนับเป็นก้าวใหม่ (ค) กด preset `1.05` แล้วแตะ slider ค่ากระโดดเป็น **1.048196** เพราะ slider ตั้ง `step="0.001"` หยาบเกินกว่าจะเก็บตำแหน่ง log scale ได้ | `"ลองทำดู"` — สั่งให้แก้ทั้ง 3 ข้อรวบเป็นรอบเดียว | (ก) เพิ่มธง `state.settled` แล้ว `return` ทันทีที่ต้น `stepOnce()` (ข) การสะกิดเขียนทับจุดล่าสุดใน `path` แทนการ `push` เพิ่ม เพราะเป็นส่วนหนึ่งของก้าวเดิมไม่ใช่ก้าวใหม่ (ค) เปลี่ยน `step` เป็น `0.00001` และแสดงผล lr ด้วยทศนิยม 5 ตำแหน่งเมื่อ lr < 0.01 · แถมแก้ปุ่ม Run ที่ค้างอยู่ที่ "Pause" ตลอดไปหลังลู่ออก **ผลทดสอบ:** steps ค้างที่ 79 ตามเดิมหลังเคาะเพิ่ม 100 ครั้ง · `path.length = steps + 1` ตรงตามที่ควรเป็น · preset ทั้ง 3 คลาดเคลื่อนน้อยกว่า 0.0032% (เดิม 1.05 คลาดเคลื่อน 0.17%) · preset ทั้ง 6 ชุดยังได้ผลเท่าเดิมทุกตัว | ข้อ (ก) รุนแรงกว่าที่เห็น เพราะ **`steps` คือตัวเลขที่แอปนี้ทั้งแอปตั้งใจจะสอน** ("lr เล็กไปใช้สเต็ปเยอะ") ถ้าตัวนับเชื่อถือไม่ได้ บทเรียนก็พังไปด้วย — บั๊กที่ดูเล็กแต่ทำลายจุดประสงค์หลัก · agent รายงาน 8 ข้อจากการทดสอบเวอร์ชันเก่า พอเอามาตรวจกับเวอร์ชันล่าสุดพบว่า **2 ข้อหายไปแล้วเองจากการแก้รอบก่อน ๆ** (การแก้เกณฑ์ลู่ออกในรอบ 2 ดักเคสแกว่งเป็นวงรอบถาวรไปด้วยโดยบังเอิญ) — **ห้ามเชื่อรายงานโดยไม่ตรวจซ้ำกับของจริง** ไม่ว่าจะมาจาก AI หรือจากคน |
| 8 ⚠️ | จบรอบ 7 แล้วอยากต่อยอดจากกราฟ 1 มิติเป็นพื้นผิว 3 มิติ แต่**ยังไม่รู้ว่าจะเขียน prompt ยังไงให้ครอบคลุม** เพราะโจทย์ใหญ่กว่าเดิมมาก (2 ตัวแปร, WebGL, กล้องหมุนได้) | `"ต่อไปจะเเก้ ui ให้มันเท่ๆสวยเเบบ 3D ขอ prompt หน่อย"` — **ขอให้ AI ร่าง prompt ให้ ไม่ได้เขียนเอง** จากนั้นตอบคำถามที่ AI ถามกลับ 2 ข้อ: เลือก "3D จริง เป็นพื้นผิว f(x,y) หมุนดูได้" และเลือกชุดฟังก์ชัน "ชาม → อานม้า → หุบเขาหลายลูก" | ได้ไฟล์ `prompt-3d.txt` ยาวราว 150 บรรทัด โครงเดียวกับ prompt แรกในส่วน B (บริบท / งาน / ข้อจำกัด / รูปแบบคำตอบ) จุดที่ต่างคือ **บทเรียนทั้ง 7 รอบข้างบนถูกแปลงเป็นข้อกำหนดล่วงหน้า** เช่น "เมื่อความชันเป็นศูนย์ ต้องยืนยันก่อนว่าเป็นจุดต่ำสุดจริง" และ "ต้องเตือนตอนที่คนดูจอยังทันเห็น" · เขียนสูตรอนุพันธ์ย่อยของทั้ง 3 พื้นผิวไว้ในตัว prompt แล้วสั่งให้ AI ตรวจสูตรซ้ำก่อนเขียนโค้ด · ปิดท้ายด้วย "อย่าเพิ่งเขียนโค้ด ถ้ามีอะไรกำกวมให้ถามก่อน" | **รอบนี้ prompt ไม่ได้มาจากตัวเอง — ให้ AI ร่างให้แล้วเอาไปใช้** ต่างจากรอบ 1–7 ที่พิมพ์เองทุกคำ · ข้อดีที่เห็นชัดคือมันหยิบบทเรียนเก่ามาใส่ได้ครบกว่าที่ตัวเองจะนึกออก (ตอบข้อ 3 ในส่วน D ได้พอดี ว่า "ถ้าย้อนไปเขียน prompt แรกใหม่จะเพิ่มอะไร") · ข้อเสียคือ**ไม่ได้ฝึกเขียน prompt เอง** ซึ่งเป็นทักษะที่วิชานี้ต้องการ · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 9 | มี prompt แล้วแต่ยังไม่มีแอป | `"D:\Kmutnb\prompt-3d.txt ทำ ui ใหม่ให้หน่อย"` — ชี้ไปที่ไฟล์ prompt ตรง ๆ | ได้ `index-3d.html` 703 บรรทัด ไฟล์เดียวจบ · AI แจ้งกลับ 2 เรื่องที่ prompt สั่งไว้แต่ทำตามไม่ได้: **OrbitControls ไม่มีบน cdnjs (404)** ต้องดึงจาก jsDelivr แทน และ preset ของ Himmelblau ต้องเป็น `0.05` ไม่ใช่ `0.03` เพราะทดสอบแล้วพบว่า 0.03 แกว่งอยู่ในกรอบจนครบ 1000 สเต็ปโดยไม่ลู่ออก **ผลทดสอบ:** เขียน `probe-3d.js` ดึงบล็อกคณิตศาสตร์จาก HTML ไปรันใน node ได้ 43/43 — รวมเทียบอนุพันธ์ทั้ง 6 ตัวกับ finite difference (คลาดเคลื่อน ~1e−10) และเคสอานม้า (0.5, 0) ที่ต้องไม่ประกาศว่าเจอคำตอบ | prompt ที่บอกให้ "ตรวจสูตรซ้ำก่อนเขียนโค้ด" กับ "ถ้าทำตามไม่ได้ให้บอก" ได้ผลจริง — AI เถียงกลับ 2 เรื่องแทนที่จะเดาเงียบ ๆ ซึ่งดีกว่าปล่อยให้ผิดแล้วมาเจอทีหลัง · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 10 | AI รายงานเองว่ามี 3 จุดที่น่าจะพัง: เส้นทางบนชามมองแทบไม่เห็น (เส้น 1px กลืนกับสีผิว) · เกณฑ์แยก "แตะ" กับ "ลาก" ตั้งไว้ 6px ซึ่งนิ้วบนมือถือสั่นเกินง่าย ๆ · กด Run แล้วสลับแท็บ ตัวเลขหยุดเดินจนดูเหมือนแอปค้าง | แปะ 3 ข้อนั้นกลับไปแล้วพิมพ์ว่า `"ไปเเก้งานให้หน่อย"` | (ก) เปลี่ยนเส้นทางเป็น **InstancedMesh ลูกปัด 3 มิติ** ไม่ใช่ TubeGeometry เพราะ tube ต้องสร้าง geometry ใหม่ทุกสเต็ป ส่วน instanced เขียน matrix ตัวเดียว (ข) แยกเกณฑ์ตาม `pointerType` — เมาส์ 6px / นิ้ว 16px / ปากกา 10px และวัด**ระยะไกลสุดตลอดการแตะ** ไม่ใช่ระยะหัว-ท้าย (ค) ฟัง `visibilitychange` แล้วหยุดเองพร้อมบอกเหตุผล + เปลี่ยนการเดินเป็นอิงเวลาจริง 120 สเต็ป/วินาที **ผลทดสอบ:** นิ้วสั่น 12px ตั้งจุดติด · ลาก 40px ไม่ติด · ลากออก 30px แล้ววกกลับ ไม่ติด · เมาส์ 12px ไม่ติด | ข้อ (ข) เป็นบั๊กที่**เทสบนคอมไม่มีวันเจอ** เพราะเมาส์นิ่งกว่านิ้วมาก · ข้อ (ค) ไม่ใช่บั๊กของโค้ดเลย เป็นพฤติกรรมปกติของเบราว์เซอร์ที่หยุดวาดภาพในแท็บที่ไม่ได้อยู่หน้าสุด แต่ผู้ใช้แยกไม่ออกจากแอปค้าง — **"ทำงานถูก" กับ "ดูเหมือนทำงานถูก" คนละเรื่องกัน** · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 11 | แอปทำงานถูกหมดแล้วแต่หน้าตายังเป็นธีมมืดแบบ default ดูไม่น่าเชื่อถือ | `"ต่อไปลองเเก้ ui ตรงส่วนอื่นๆให้มันสวยๆ เน้นขาวๆคลีนๆให้งานง่ายๆ ... เเบบประมาณนี่"` — **แนบสกรีนช็อตหน้าเว็บที่ชอบมาให้ดูด้วย** (หน้า landing ของ RothFinder: พื้นลาเวนเดอร์ การ์ดขาวมุมโค้ง ขอบดำเส้นบาง ปุ่มแคปซูล) | เปลี่ยนเป็นธีมสว่างทั้งหมด · สิ่งที่ลืมไม่ได้คือ**สีในฉาก 3D ต้องเปลี่ยนตามด้วย** ไม่งั้นเส้นกริดสีขาวหายเกลี้ยงบนพื้นขาว ต้องไล่เปลี่ยนพื้นหลัง canvas เส้นกริด และลูกศร **บั๊กที่เจอ:** ปุ่ม Run **หายไปทั้งปุ่ม** ตอนถูก disable เพราะกฎ `.pill.primary` ตั้งตัวอักษรเป็นสีขาว แล้วกฎ disabled เปลี่ยนพื้นเป็นขาวแต่ลืมเปลี่ยนสีตัวอักษรกลับ — ขาวบนขาว จับได้ตอนดูสกรีนช็อต | **แปะรูปให้ดูได้ผลดีกว่าบรรยายเป็นคำ** — หลักการเดียวกับรอบ 2/3/4 ที่แปะตัวเลขจากหน้าจอไปให้ดู แค่เปลี่ยนจากตัวเลขเป็นรูป · บั๊กปุ่มขาวบนขาวเป็นบั๊กประเภทที่ต้อง**เปิดดูด้วยตา**ถึงจะเจอ เพราะโค้ดไม่ผิดสักบรรทัด · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 12 | อยากได้ไอเดียดีไซน์เพิ่มจากงานจริงของคนอื่น | `"https://dribbble.com/tags/ui-design ไปเปิดเว็ปนี่เเล้วลองไปเหลือ design มาสวยๆ"` | เข้าหน้า tag ได้ แต่หน้า search **เด้ง CAPTCHA** และ tag `data-visualization` redirect กลับหน้าแรก · รอบแรก AI ดูแค่หน้าจอแรกแล้วสรุปว่า "เป็นงาน dark เกือบหมด ไม่ตรงโจทย์" พอสั่งให้ไล่ดูลึกกว่านั้นจึงเจอ 3 ชิ้นที่ใช้ได้ → หยิบมาทำ **กราฟ f ต่อ step (convergence curve)** ใส่ในแถบตัวเลข, **ป้ายบอกว่า f ขึ้นหรือลงเท่าไรในก้าวล่าสุด** (เขียว/แดง) และเปลี่ยนปุ่มเลือกฟังก์ชันเป็น**การ์ดมีสีพร้อมคำอธิบาย** | AI **สรุปเร็วเกินไปในรอบแรก** ถ้าเชื่อคำว่า "ไม่มีอะไรตรงโจทย์" ก็จะไม่ได้อะไรกลับมาเลย ต้องสั่งให้ดูให้ครบก่อนค่อยสรุป · ของที่ได้มาไม่ใช่แค่สวย — convergence curve คือกราฟที่คอร์ส ML วาดกันจริง ๆ กลายเป็นทั้งของสวยและของสอน · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 13 | ยังไม่พอใจกับหน้าตา อยากให้ AI ตัดสินใจเรื่องดีไซน์เองแทนที่จะลอกเรฟ | `"เอางี้ลอง design เองเลยงั้นขอสวยกว่านี่"` | AI ไปอ่านแนวทางการทำ data visualization ก่อน แล้วพบว่า**แรมป์สีเดิมของแอป (น้ำเงิน→เขียว→เหลือง→แดง) คือ rainbow ซึ่งเป็น anti-pattern** — กฎคือ sequential ต้องเฉดเดียวไล่อ่อน→เข้ม · เปลี่ยนเป็น **ฟ้าเฉดเดียวแมปตามความลึก** (ยิ่งเข้ม = ยิ่งต่ำ สายตาจึงวิ่งไปหาคำตอบเอง) + **ส้ม** เป็นสีเดียวของตัวเดิน · รันเครื่องมือตรวจคู่สีจริงได้ค่าแยกแยะสำหรับคนตาบอดสี 24.7 เทียบเกณฑ์ 8 · เขียน **marching squares** เองเพื่อวาดเส้น contour แทนตารางสี่เหลี่ยม | ของเดิมที่คิดว่า "สีเยอะ = อ่านง่าย" กลับผิดหลัก เพราะ rainbow ทำให้สมองอ่านลำดับผิด · เส้น contour ได้มาทั้งความสวยและความรู้ เพราะ **gradient ตั้งฉากกับเส้นระดับเสมอ** ซึ่งเป็นข้อเท็จจริงที่มองไม่เห็นในกราฟ 1 มิติ · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 14 | ชื่อพื้นผิวกับศัพท์ในแอปเป็นภาษาไทยล้วน ("ชาม", "อานม้า", "จุดต่ำสุด", "ความชัน") ซึ่งไม่ตรงกับคำที่ต้องใช้จริงตอนอ่านเปเปอร์หรือเรียน ML | `"อานม้า ชาม เปลี่ยนเป็นชื่อ ภาษาอังกฦษให้หน่อย"` แล้วตามด้วย `"ไปเปลี่ยนเป็นคำนศัพท์คณิตศาสตร์ภาษาอังกฦษให้ด้วย"` | แก้ 26 จุด: จุดต่ำสุด → minimum · จุดอานม้า → saddle point · ความชัน → gradient · หุบเขา → basin · ลู่เข้า/ลู่ออก → converge/diverge **ผลข้างเคียงที่ไม่คาดคิด:** `probe-3d.js` **พังทันที 3 เคส** เพราะไฟล์ทดสอบยังค้นหาคำไทยเดิมอยู่ ต้องไล่แก้ให้ตรงคำใหม่ · ระหว่างไล่แก้ยังเจอคำอธิบายที่**ไม่จริงแล้ว**ค้างอยู่ด้วย: "เม็ดสีชมพูคือทางที่เดินผ่านมา" (เปลี่ยนเป็นส้มตั้งแต่รอบ 13) และ "สีเย็น = ต่ำ สีร้อน = สูง" (ตอนนี้เป็นฟ้าเฉดเดียว) | การเปลี่ยนคำที่ดูเหมือนงานเล็กที่สุดกลับ**ทำให้ชุดทดสอบพัง** เพราะ test ไปผูกกับข้อความที่ผู้ใช้เห็น — ถ้าไม่มี probe คงไม่รู้เลย · และพบว่า**คำอธิบายเน่าตามหลังโค้ดโดยไม่มีอะไรเตือน** เปลี่ยนสีไปแล้ว 2 รอบแต่ข้อความยังบอกสีเก่า · 【เขียนทับด้วยความเห็นตัวเอง】 |
| 15 ★ | แก้มาหลายรอบจนไม่แน่ใจว่ายังมีข้อความไหนขัดกับสิ่งที่โปรแกรมทำจริงอีกบ้าง | `"ไปเเบ่งงานให้ agent update ของใหม่เข้าไปให้หน่อย"` — สั่งให้กระจายงานเหมือนรอบ 5 | AI **ปฏิเสธที่จะแบ่งงานส่วนที่แก้ไฟล์** โดยให้เหตุผลว่า agent หลายตัวเขียนไฟล์เดียวกันพร้อมกันจะทับกันเอง แล้วเสนอให้แบ่งเฉพาะ**การตรวจ** แบบอ่านอย่างเดียว — ตัวหนึ่งดูเฉพาะ JS อีกตัวดูเฉพาะ HTML · ได้กลับมา 17 จุด ที่หนักสุดคือ **2 ข้อความที่ขัดกับสิ่งที่เห็นบนจอ**: (ก) Saddle ที่ lr น้อย ลูกบอลเกาะสัน y = 0 แล้วหยุดนิ่ง แต่แอปบอกว่า "จะไถลลงไปเรื่อย ๆ ไม่มีวันหยุด" (ข) Saddle ที่ lr = 1.05 แอปบอก "ถูกต้องแล้ว ไม่ใช่บั๊ก" ทั้งที่แกน x ก็กระเด็นออกเพราะ lr ใหญ่เกินจริง ๆ — เป็นการแก้ตัวให้ค่าที่ผิด · ยังเจอด้วยว่า "Bowl มี minimum เดียว ไปยังไงก็ถึง" **ขัดกับบรรทัดเหนือมันเอง**ที่บอกว่า preset 1.05 ทำให้หลุดกรอบ **ผลทดสอบ:** เพิ่มเคสกันบั๊ก 2 ตัวนั้น probe เป็น 46/46 | **สั่งให้แบ่งงานแล้วโดนเถียงกลับว่าแบ่งไม่ได้** ซึ่งถูกของมัน — งานเขียนไฟล์เดียวกันขนานกันไม่ได้ แต่งาน**อ่าน**ขนานได้ · บทเรียนที่ตรงกับรอบ 5: ที่ทำให้ได้ผลคือ**แยกขอบเขตให้ชัด** ตัวหนึ่งห้ามแตะ HTML อีกตัวห้ามแตะ JS · และพบว่าบั๊กที่เหลือในตอนท้ายไม่ใช่บั๊กของโค้ดแล้ว แต่เป็น**บั๊กของคำอธิบาย** ซึ่งไม่มี compiler หรือ test ตัวไหนจับให้ · 【เขียนทับด้วยความเห็นตัวเอง】 |

> **หมายเหตุสำคัญเรื่องที่มาของ prompt ในรอบที่ 8 เป็นต้นไป**
> รอบที่ 1–7 เป็น prompt ที่พิมพ์เองทุกคำ ส่วนรอบที่ 8 เป็นการ**ขอให้ AI ร่าง prompt ให้** แล้วนำไปใช้
> รอบที่ 9–15 กลับมาเป็น prompt ที่พิมพ์เองอีกครั้ง (บันทึกไว้ตามที่พิมพ์จริง รวมทั้งคำที่สะกดผิด)
> บันทึกไว้ตรงนี้เพื่อให้เห็นชัดว่าส่วนไหนเป็นฝีมือตัวเองและส่วนไหนไม่ใช่

**หมายเหตุ:** รอบที่ 1 คือรอบ debug ที่ใบงานบังคับให้ต้องมีอย่างน้อย 1 รอบ
รอบนี้น่าสนใจเป็นพิเศษเพราะ **ไม่ใช่บั๊กของโค้ดในความหมายทั่วไป** (ไม่ได้พิมพ์สูตรผิดหรือมี syntax error
โค้ดคำนวณอนุพันธ์ถูกทุกตัวและอัปเดตค่าตามสูตร `x ← x − lr·f′(x)` ถูกต้อง) แต่เป็น**ความเข้าใจผิดทางคณิตศาสตร์
ที่ฝังอยู่ในเงื่อนไขหยุด**: AI เขียนว่า "ถ้า |f′(x)| < 1e−7 ให้ถือว่าเจอจุดต่ำสุด" ซึ่งอ่านแล้วดูสมเหตุสมผลมาก
แต่ผิด เพราะ f′(x) = 0 บอกได้แค่ว่าถึง **จุดวิกฤต (stationary point)** เท่านั้น ซึ่งอาจเป็นจุดต่ำสุด จุดสูงสุด
หรือจุดอานม้าก็ได้ ต้องดู f″ หรือชิมค่า f สองข้างถึงจะแยกออก

เคสที่ทำให้โผล่มาก็บังเอิญมาก: `x⁴ − 3x² + 1` เริ่มที่ x = −2.0 ด้วย lr = 0.1 ได้
`x₁ = −2 − 0.1(−20) = 0` **พอดีเป๊ะ** ซึ่ง f′(0) = 0 แต่ f″(0) = −6 แปลว่าเป็น**จุดสูงสุดเฉพาะที่**
แอปจึงประกาศว่าเจอคำตอบแล้วที่จุดที่สูงกว่าคำตอบจริงถึง 2.25 (จุดต่ำสุดจริงอยู่ที่ x = ±1.2247, f = −1.25)

จับได้เพราะ**เทียบกับสิ่งที่เรียนมา** ไม่ใช่เพราะเห็นด้วยตา — บนจอมันดูเหมือนทำงานถูกทุกอย่าง ลูกบอลไหลลงเขา
แล้วหยุดนิ่งอยู่ตรงกลางกราฟ ถ้าไม่รู้ว่า f″ ใช้ทำอะไรก็จะผ่านตาไปเฉย ๆ

ส่วนเรื่อง learning rate ใหญ่เกินขีดจำกัดเสถียรภาพ (โดยประมาณคือมากกว่า 2/f″(x) สำหรับฟังก์ชันกำลังสอง)
จนค่าลู่ออกและล้นเป็น Infinity อยู่ในรอบที่ 2 ซึ่ง AI **ดักไว้ให้แล้วตั้งแต่ prompt แรก** เพราะระบุเรื่องนี้ไว้
แต่ตั้งเกณฑ์ที่ `\|x\| > 1e6` ซึ่งหลวมเกินกว่าที่ตาคนจะทัน — เป็นตัวอย่างว่า "AI ทำตามที่สั่งแล้ว" กับ
"ใช้งานได้จริง" ไม่ใช่เรื่องเดียวกัน
---

## ส่วน D — สรุปสิ่งที่ได้เรียนรู้

**1. Prompt แบบไหนที่ได้ผลดีกับคุณ และแบบไหนที่เสียเวลาเปล่า**

prompt ที่ได้ผลดีที่สุดกับผมคือตอนรอบ 5 ที่พิมพ์ว่า "กระจายงานให้ agent ทำให้ทำให้เร็วขึ้น" ซึ่งจริง ๆ
เป็น prompt ที่กว้างมากและไม่ได้ระบุเลยว่าให้ไปหาบั๊กอะไร แต่กลับได้ผลดีที่สุดในงานนี้ เพราะตอนนั้น
ผมไม่รู้ว่าเหลือบั๊กอะไรอยู่บ้าง สิ่งที่ต้องการจึงเป็น "การสำรวจ" ไม่ใช่ "การแก้จุดที่รู้อยู่แล้ว" —
prompt กว้างจึงเหมาะกับงานแบบนี้ ผลคือได้บั๊กที่กดเล่นด้วยเมาส์ยังไงก็ไม่มีทางเจอกลับมา 3 รอบติด
(รอบ 5 คีย์บอร์ดโดน keydown ระดับ window แย่งไป, รอบ 6 clamp ที่วางผิดที่จน Reset ไม่ช่วย,
รอบ 7 ตัวนับ steps เดินต่อหลังจบไปแล้ว 79 → 179) สิ่งที่ทำให้มันได้ผลไม่ใช่ความยาวของ prompt
แต่เป็นการบังคับให้ agent **แยกขอบเขตกันชัด ๆ** (ตัวหนึ่งดูแต่ตัวเลข อีกตัวดูแต่ UI) และบังคับให้แยกว่า
ข้อไหน "ยืนยันแล้ว" ข้อไหน "ยังต้องเปิดเบราว์เซอร์ดู" ถ้าไม่สั่งสองอย่างนี้จะได้รายงานซ้ำกันและแยกไม่ออก
ว่าอะไรจริง เทียบกับ prompt แรกในส่วน B ที่ยาวและมีครบ 5 องค์ประกอบ อันนั้นได้โครงที่ใช้งานได้จริงตั้งแต่
รอบเดียว แต่มันช่วยได้แค่ "ตั้งต้น" ไม่ได้ช่วยหาสิ่งที่ตัวเองมองไม่เห็น ซึ่งเป็นคนละหน้าที่กัน

ส่วนที่เสียเวลาเปล่ากลับมาจาก prompt ตัวเดียวกันนั้นเอง prompt กระจาย agent ให้ผลกลับมา 23 ข้อ
(ฝั่ง UI 15 ข้อ ฝั่งอัลกอริทึม 8 ข้อ) แต่สุดท้ายผมแก้จริงแค่ประมาณ 6 ข้อ ที่แย่กว่านั้นคือพอเอา 8 ข้อ
ของฝั่งอัลกอริทึมมาตรวจกับเวอร์ชันล่าสุด พบว่า 2 ข้อ**หายไปเองแล้ว**จากการแก้รอบก่อน ๆ เพราะ agent
ไปทดสอบกับเวอร์ชันเก่า (การแก้เกณฑ์ลู่ออกในรอบ 2 ดันไปดักเคสแกว่งเป็นวงรอบถาวรด้วยโดยบังเอิญ)
แปลว่าเวลาที่เสียไปกับการอ่านและไล่ตรวจรายงานสองข้อนั้นไม่ได้อะไรกลับมาเลย ผมเลยสรุปได้ว่า
prompt ที่กว้างแบบนี้ให้ทั้งของดีที่สุดและของเสียเปล่ามากที่สุดพร้อมกัน มันเหมาะกับตอนที่ยังไม่รู้ว่า
ต้องหาอะไร แต่ต้นทุนคือ**ต้องมานั่งคัดเองว่าอะไรจริงอะไรไม่จริง** ถ้าจะใช้ซ้ำ สิ่งที่ต้องเพิ่มเข้าไปใน prompt
คือระบุให้ชัดว่าให้ตรวจกับไฟล์เวอร์ชันไหน ไม่ใช่ปล่อยให้ agent ไปหยิบตัวที่มันเจอเอง

**2. มีจุดไหนที่ AI เขียนโค้ดผิดหรือมั่วบ้าง คุณรู้ได้อย่างไร**

เจอทั้งหมดสามทาง ทางแรกคือ **เทียบกับสิ่งที่เรียนมา** ซึ่งเป็นเคสที่ร้ายที่สุด (รอบ 1) AI เขียนเงื่อนไขหยุดว่า
"ถ้า |f′(x)| < 1e−7 ให้ถือว่าเจอจุดต่ำสุด" ซึ่งอ่านแล้วดูสมเหตุสมผลมาก โค้ดคำนวณอนุพันธ์ก็ถูกทุกตัว
ไม่มี syntax error ไม่มีอะไรเด้งใน console และบนจอลูกบอลก็ไหลลงเขาแล้วหยุดนิ่งเหมือนทำงานถูกทุกอย่าง
แต่ผมรู้จากที่เรียนมาว่า f′(x) = 0 บอกได้แค่ว่าถึงจุดวิกฤต ต้องดู f″ ถึงจะแยกออกว่าเป็นจุดต่ำสุดหรือสูงสุด
พอลองเคส x⁴−3x²+1 เริ่มที่ −2.0 ด้วย lr = 0.1 มันหยุดที่ x = 0 พอดีเป๊ะ ซึ่ง f″(0) = −6 คือ**จุดสูงสุด**
แอปประกาศว่าเจอคำตอบที่จุดที่สูงกว่าคำตอบจริงถึง 2.25 ถ้าไม่รู้ว่า f″ ใช้ทำอะไรก็จะผ่านตาไปเฉย ๆ
ทางที่สองคือ **เห็นด้วยตา** เช่นรอบ 2 ที่จอว่างเปล่าอยู่ 133 สเต็ปโดยไม่บอกอะไรเลย และรอบ 4 ที่ขึ้นว่า
x = −0.0000 ซึ่งเป็นลบศูนย์ที่ไม่มีความหมายทางคณิตศาสตร์ — สองอันนี้ AI ไม่มีทางเจอเองเพราะมันไม่ได้มองจอ
ทางที่สามคือ **ให้ agent ไล่อ่านโค้ดแทน** ซึ่งได้บั๊กที่ทั้งตาและความรู้คณิตศาสตร์จับไม่ได้ อย่างตัวนับ steps
ที่เดินต่อหลังจบแล้ว ต้องเคาะ Spacebar ซ้ำ 100 ครั้งถึงจะเห็น ข้อนี้รุนแรงกว่าที่ดู เพราะ steps คือตัวเลขที่
แอปนี้ทั้งแอปตั้งใจจะสอน ถ้ามันเชื่อถือไม่ได้บทเรียนก็พังไปด้วย · จุดที่ตอนแรกไม่รู้แล้วมารู้ทีหลังคือรอบ 1
เพราะกดเล่นมาหลายรอบก่อนหน้านั้นแล้วโดยไม่เอะใจ

**3. ถ้าย้อนเวลากลับไปเขียน prompt แรกใหม่ คุณจะเขียนว่าอย่างไร**

จะเพิ่มสี่เรื่อง และคิดว่าตัดรอบแก้บั๊กไปได้ประมาณ 4 รอบจาก 7 รอบ เรื่องแรกคือต้องเขียนให้ชัดว่า
**"เมื่อความชันเป็นศูนย์ ต้องยืนยันก่อนว่าเป็นจุดต่ำสุดจริง ไม่ใช่จุดสูงสุดหรือจุดอานม้า"** ตัดรอบ 1 ออกได้เลย
และเป็นบั๊กที่ร้ายที่สุดที่มาจากช่องโหว่ของ prompt แรกตรง ๆ เรื่องที่สองคือเกณฑ์การเตือน — prompt แรก
เขียนแค่ว่าให้กัน Infinity/NaN ซึ่ง AI ก็ทำตามแล้วจริง ๆ (ตั้งไว้ที่ |x| > 1e6) แต่ควรเขียนว่า
**"ต้องเตือนตอนที่คนดูจอยังทันเห็น"** และควรระบุเพดานจำนวนสเต็ปไว้ด้วย ตัดรอบ 2 กับ 3 ออกได้
ตอนเขียน prompt แรกผมนึกถึงแต่กรณี "lr ใหญ่เกินไปจนพัง" ไม่ได้นึกเลยว่า "lr เล็กเกินไปจนน่าเบื่อ"
ก็เป็นปัญหา ทั้งที่มันคือครึ่งหนึ่งของสิ่งที่แอปนี้ตั้งใจจะสอน เรื่องที่สามคือกฎการแสดงผลตัวเลข
(ห้ามมีลบศูนย์ ทศนิยมกี่ตำแหน่ง ค่าเล็กมากให้แสดงยังไง) ตัดรอบ 4 ออกได้ เรื่องที่สี่คือคีย์บอร์ด —
prompt แรกเขียนว่า "ต้องรองรับการควบคุมด้วยคีย์บอร์ด" ซึ่งกว้างเกินไป AI เลยทำแบบที่ไปแย่งลูกศร
จาก slider และ radio จนฟังก์ชันที่ 2 กับ 3 เข้าถึงด้วยคีย์บอร์ดล้วนไม่ได้เลย ทั้งที่ทำตามที่สั่งแล้ว
ควรเขียนให้ชัดว่าตัวควบคุมมาตรฐานต้องจัดการคีย์ของตัวเองได้ · โดยรวมคิดว่า prompt แรกยาวกำลังดี
ปัญหาไม่ได้อยู่ที่ความยาว แต่อยู่ที่มันครอบคลุมแต่เรื่องที่ผมนึกออก ซึ่งไม่ใช่เรื่องที่พังจริง

**4. งานนี้เปลี่ยนมุมมองของคุณต่อการใช้ AI เขียนโค้ดอย่างไร**

ก่อนทำงานนี้ผมคิดว่าหน้าที่ของคนใช้คือสั่งให้ครบ ๆ แล้วรอรับโค้ด ทำเสร็จแล้วมุมมองเปลี่ยนไปว่าบทบาท
ของผมคือ **คนตรวจและคนออกแบบเงื่อนไข** ไม่ใช่คนสั่งงาน เพราะงานหนักจริง ๆ ของงานนี้ไม่ได้อยู่ที่ตอน
เขียนโค้ด — AI เขียนโค้ดที่รันได้ให้ตั้งแต่รอบแรก ไม่มี syntax error สักครั้งตลอด 7 รอบ — แต่อยู่ที่ตอน
ตัดสินใจว่าอะไรถูกอะไรพอ ตัวอย่างที่ชัดที่สุดคือเพดาน MAX_STEPS = 1000 ในรอบ 3 ซึ่งไม่ใช่เรื่องที่มี
คำตอบถูกผิดทางคณิตศาสตร์ มันคือการตัดสินใจเชิงออกแบบว่าจะให้คนรอนานแค่ไหน AI ตัดสินใจแทนไม่ได้
ถ้าไม่บอก และรอบ 6 ที่ agent รายงานมา 15 ข้อแต่ผมเลือกแก้แค่ข้อที่รุนแรงจริง — การตัดสินใจว่า
**อะไรไม่ต้องแก้** ก็เป็นงานของคนเหมือนกัน อีกอย่างที่ได้จากรอบ 7 คือห้ามเชื่อรายงานโดยไม่ตรวจซ้ำ
agent รายงานมา 8 ข้อ พอเอามาตรวจกับเวอร์ชันล่าสุดเหลือของจริง 3 ข้อ อีก 2 ข้อหายไปเองแล้วจากการ
แก้รอบก่อน ๆ ถ้าเชื่อทั้งดุ้นก็จะไปแก้ของที่ไม่ได้พัง ถ้าต้องทำโปรเจกต์ ML ที่ใหญ่กว่านี้ สิ่งที่จะทำต่างไป
คือจะวางวิธีตรวจไว้ก่อนตั้งแต่ต้น ไม่ใช่รอให้เห็นว่าพังแล้วค่อยไปหา เพราะงาน ML ผลลัพธ์ที่ผิดมักจะ
"ดูสมเหตุสมผล" เหมือนเงื่อนไข |f′| < 1e−7 ในรอบ 1 ซึ่งเป็นแบบที่อันตรายที่สุด

## Checklist ก่อนส่ง

- [x] เปิดลิงก์แอปใน Incognito แล้วใช้งานได้จริง (ไม่ต้อง login ไม่มีหน้าจอ error) — หน้าเว็บเป็นไฟล์เดียวจบ ไม่มี external script/font/fetch/localStorage/cookie เลย จึงไม่มีอะไรให้ login หรือให้ Incognito บล็อก; โหลดใหม่แล้ว console ไม่มี error
- [x] ทดลองเปิดแอปบนมือถือจริง (หรือโหมดจำลองมือถือใน DevTools) แล้วใช้งานได้ — ทดสอบที่ความกว้าง 390px: ไม่ล้นจอแนวนอน, canvas ย่อเป็น 314×260, ตัวเลขห่อเป็น 2×2, กด Run ได้จริงและลู่เข้า x=0 ที่ 79 สเต็ป
- [x] มีการโต้ตอบ (interaction) ครบอย่างน้อย 3 อย่าง (เช่น slider, ปุ่ม, คลิกลากบน canvas)
- [x] มีคำอธิบายวิธีใช้งานแสดงอยู่ในหน้าเว็บจริง ไม่ใช่แค่ใน README
- [x] Prompt Log มีครบทั้ง 4 ส่วน (A–D)
- [x] มีบันทึกการปรับแก้อย่างน้อย 5 รอบ และมีรอบ debug อย่างน้อย 1 รอบ
- [x] ตั้งสิทธิ์ Google Doc เป็น "ผู้ที่มีลิงก์ = ผู้อ่าน" แล้วลองเปิดลิงก์ใน Incognito เพื่อยืนยัน — ตรวจผ่าน Drive API ได้ `{"role":"reader","type":"anyone"}` เจ้าของ kimkim3317738@gmail.com
  Doc: https://docs.google.com/document/d/180By5hEEuDsZgW1258KKIV2_tG8usuqsPuU2i2drV_o/edit
- [x] GitHub repo ตั้งเป็น Public และไม่มี API key หรือข้อมูลลับใด ๆ อยู่ในโค้ด
- [ ] ส่งครบทั้ง 3 ลิงก์ (แอปที่ deploy แล้ว / Google Doc "Prompt Log" / GitHub repo) ใน Google Classroom
